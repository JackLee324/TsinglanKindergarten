QLS — B6 FINAL: ROOT-CAUSED, FIXED, PROVEN BYTE-IDENTICAL (5 runs)
generated: 2026-09-24T03:54:25Z

SECOND, DEEPER PASS TOOK OVER THE EARLIER PARTIAL FIX (my overlap: two agents
were assigned B6; the second went further and removed the first's workaround).

ROOT CAUSES (measured, not assumed) - three interference mechanisms plus a
fourth defect nobody had reported:
  (a) teachers.permissions_version is ACCOUNT-WIDE. The DB trigger bumps it on
      any roles/status change, and AuthGuard turns a mismatch into 401 AND
      destroys the session IN EVERY PROCESS. So one suite demoting an account
      is indistinguishable, from another process, from its own session being
      revoked. Server log pinned it:
        WARN [AuthGuard] Session invalidated by permission change:
          teacher=378b8d2a-... sessionVersion=147 currentVersion=148
  (b) resetFixtures() ran an unconditional 'update sessions set revoked = true',
      logging out EVERY live suite at once (REVOKED:test_fixture_reset).
  (c) one literal probe-title prefix + a LIKE-based cleanup: a second run
      deleted the first run's probe row ('0 expected 1' / 404).
  (d) NEW, unreported: verify-files-http.mjs ended with
      process.exit(fail ? 1 : 0) INSIDE finally. process.exit there terminates
      before the in-flight exception propagates, so an ABORTED suite printed
      'pass=22 fail=0' and EXITED 0 - a broken suite could look green.
      This is a gate-integrity defect and the most dangerous of the four.

FIX: removed the committed 401 retry (a retry cannot distinguish 'the
environment moved under me' from 'this endpoint really stopped accepting my
session', so it hides the very regressions the suite exists to catch). Suites
now OWN their state: per-run fixture accounts, per-run probe rows, scoped
cleanup that verifies nothing survived, strict original cover assertion, no
swallowed exceptions, and a PostgreSQL advisory GATE LOCK (exclusive for
verify-all.sh, shared for standalone suites) turning a destructive overlap
into a named environment error instead of a fake cascade.
No assertion was deleted or relaxed: label sets match the pre-workaround
baseline 44d1fbf exactly (authz 24 / hardening 10 / mfa 36 / files-http 72).

PROOF: 5 consecutive full-gate runs with source hash AND server PID unchanged
throughout, byte-identical logs (md5 268c4fe75aa9cb1d82785a9de4778409).
Zero leftover per-run accounts, probe rows or locks. qlsadmin's
permissions_version FROZEN at 167 (it was still climbing every run before) -
direct proof the suites no longer mutate shared account state.

MY INDEPENDENT RE-RUN (11:53, clean):
  npm test 183/183 · typecheck server PASS · typecheck client PASS · build PASS
  api-contracts matched · authz-http 24/0 · hardening 10/0 · mfa 36/0
  security-headers 20/0 · files-http 73/0   ->  全部通过
  (B4's naming refactor has landed: typecheck is green again.)

REGRESSION GUARD: scripts/verify-gate-reproducible.sh, five guards each mapped
to a failure actually observed: dirty-state double gate (byte-identical),
crash-must-exit-non-zero, two concurrent files-http runs, gate-lock refusal,
and a static check that the removed retry does not return.

ENVIRONMENT-DEPENDENT COUNT, documented as a precondition in verify-all.sh:
  files-http is 73 checks with DOWNLOAD_TOKEN_TTL_SECONDS=10 and 71 with the
  app default (300 s), because the live expiry wait exceeds what the suite will
  wait. It prints a NOTE naming the skipped checks; it never silently passes.

EXPLAINS AN OLD MYSTERY: verify-authz-http.mjs's login guard tested
status !== 200 while a successful login returns 201, so it printed
'LOGIN FAILED ... 201' on EVERY successful login. That is the confusing output
I saw in round 7 and could not explain at the time. Now fixed.
